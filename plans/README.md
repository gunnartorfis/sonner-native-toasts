# Implementation Plans

Generated by the `improve` skill on 2026-06-16, against commit `2c3ff4c`.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row in the table below when done.

These plans are self-contained. They were written for an executor with **zero
context** from the advisor session — everything needed is in the plan file.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add `AGENTS.md` for contributors & agents | P1 | S | — | TODO |
| 002  | Fix unbounded `toastRefs` growth in the toast store | P1 | S | — | TODO |
| 003  | Re-enable the excluded `areToastsEqual` test | P1 | S | — | TODO |
| 004  | Fix documentation drift (defaults, `center`, missing props) | P2 | S | — | TODO |
| 005  | Use reactive window width (fix stale `Dimensions.get`) | P2 | M | — | TODO |
| 006  | Add component render tests for `toast.tsx` / `toaster.tsx` | P3 | L | 002, 003 (soft) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- No plan has a *hard* dependency on another — each can be executed independently.
- **006 is soft-ordered after 002 and 003**: it introduces a component-test
  harness; running it on an already-green suite makes failures easy to attribute.
  Land 002/003 first so the unit suite is clean before adding render tests.
- 001 has no code dependency but is listed first because the `AGENTS.md` it
  creates documents the build/test/lint commands and conventions every other
  plan's executor relies on.

## Findings considered and rejected (do not re-audit)

- **`resumeTimer` remaining-time math** (`src/toast-store.ts:135-141`): drops the
  `ENTERING_ANIMATION_DURATION` offset and floors the resumed timer at 1000ms.
  Real but cosmetic (only affects `pauseWhenPageIsHidden` resume timing by a few
  hundred ms). Low leverage — not worth a dedicated plan.
- **`toast-fns.ts` spread-order inconsistency** (`toast.info` spreads `options`
  before `variant`; siblings spread after): no functional bug because
  `ExternalToast` omits `title`/`variant`, so options can't override them. Pure
  style nit; leave it.
- **`pnpm audit` advisories** (1 critical / 10 high): all live in the `docs/`
  Docusaurus workspace and dev-only `@babel/core` — none in the published library
  or its build/distribution path. The shipped surface (a UI component, no
  network/eval/filesystem/secrets) is clean. Not actionable as a library concern;
  bump the docs toolchain opportunistically if desired.
